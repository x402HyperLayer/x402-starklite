# x402-starklite
Lightweight STARK proof ingestion, validation, signature verification, and verifier client for the x402 HyperLayer.   Includes proof schema, batch validator, sqlite storage, and REST/CLI ingestion flow.   Designed for developers building cryptographic proof pipelines with minimal overhead.
